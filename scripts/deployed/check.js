const fs = require("fs");
const path = require("path");


const filepath = path.join(
        __dirname, 
        `../../marketplace-contracts/dev.qa.marketplaceAddress.json`
    );


console.log(filepath);
